//! Harness TUI entry. Iter 2 — MVP CLI runs in line mode only.

#![forbid(unsafe_code)]

/// Launch the TUI. Iter 2.
pub fn launch() -> anyhow::Result<()> {
    anyhow::bail!("harness-tui not yet implemented (iter 2)")
}
